server:
  port: ${SERVER_PORT:8080}

spring:
  cloud:
    gateway:
      mvc:
        routes:
          - id: user-service-auth
            uri: ${USER_SERVICE_URL:http://localhost:8083}
            predicates:
              - Path=/api/auth/**

          - id: user-service-users
            uri: ${USER_SERVICE_URL:http://localhost:8083}
            predicates:
              - Path=/api/users/**

          - id: core-service-social
            uri: ${CORE_SERVICE_URL:http://localhost:8081}
            predicates:
              - Path=/api/feed/**,/api/posts/**,/api/modules/**

          - id: support-service-notifications
            uri: ${SUPPORT_SERVICE_URL:http://localhost:8082}
            predicates:
              - Path=/api/notifications/**

management:
  endpoints:
    web:
      exposure:
        include: health,info